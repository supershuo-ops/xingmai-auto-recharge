# 星脉自动充值 · 操作手册

适用版本：5.9.3

这个脚本装在浏览器里，帮你盯着京小洁的子账号余额和投产，符合条件就自动去京准通充值，并按你设的预算控制当天花多少。

按本手册从头做一遍，大约 10 分钟。

---

## 一、它能做什么

- 每 10 分钟自动看一次京小洁的子账号：余额低、投产好，就自动充值
- 当天总共能充多少，由你设的预算说了算，不会一次充爆
- 可以按时段控制节奏，比如上午最多只花当天预算的 15%
- 可以把子账号分组（搜索类、推荐类等），给每组单独设预算
- 也可以给某个账号单独设当天上限
- 每次充没充、为什么没充，都能在「提交记录」里查到
- 充值后可以发钉钉通知

**它不会做的事**：不会改你的出价和计划，不会动预算之外的钱，也不保证平台一定到账（以京准通后台记录为准）。

---

## 二、安装

1. 浏览器先安装 **Tampermonkey**（油猴）扩展。Chrome 用户还要在扩展详情页打开 **允许用户脚本 / 开发者模式**，否则脚本不会运行。

2. 打开安装地址：

   https://raw.githubusercontent.com/supershuo-ops/xingmai-auto-recharge/main/jxj-yanyuan-auto-recharge.user.js

3. 在弹出的油猴页面点 **安装**。

4. 打开油猴的「管理面板」，如果列表里还有 **星脉自动充值V5.5.x** 或 **终版测试2-规则面板版** 这类旧脚本，删掉，只留 **星脉自动充值** 一个。留两个会互相抢着充值。

5. 打开京小洁「广告投放明细」页面，右下角应出现蓝色的 **工作台 5.9.3** 按钮。点它打开工作台。

看不到按钮的处理办法见第八节。

> 以后不用再手动下载。你的负责人更新脚本后，油猴会自动更新，你原来的设置都会保留。

---

## 二之二、安装失败怎么办

### 情况一：停在 Tampermonkey 的英文页面，没有出现安装按钮

页面上写着 `This page is used as an intermediate step to install a new userscript`，然后就不动了。

这是油猴的中转页。它没跳转，说明这个浏览器里的油猴没能接手安装。按顺序查：

**1. 确认油猴装了并且是开启的**

地址栏输入 `chrome://extensions`，找到 Tampermonkey，右下角开关必须是**蓝色**。没有这个扩展就先装扩展，再重新点安装链接。

**2. 打开开发者模式**

还在 `chrome://extensions` 这一页，**右上角「开发者模式」打开**。新版 Chrome 不打开这个，油猴既装不上脚本也运行不了脚本。打开后刷新页面重试。

**3. 换一个入口装**

油猴图标 → **管理面板** → **实用工具** → 在「从 URL 导入」里粘贴下面这个地址，点导入：

```
https://raw.githubusercontent.com/supershuo-ops/xingmai-auto-recharge/main/jxj-yanyuan-auto-recharge.user.js
```

### 情况二：上面都试过还是不行 —— 手动粘贴安装

这个办法一定能成：

1. 浏览器打开这个地址（前面带 `view-source:`）：

   ```
   view-source:https://raw.githubusercontent.com/supershuo-ops/xingmai-auto-recharge/main/jxj-yanyuan-auto-recharge.user.js
   ```

2. `Ctrl+A` 全选，`Ctrl+C` 复制
3. 油猴图标 → **添加新脚本…**
4. 编辑器里 `Ctrl+A` 全选删掉默认内容，`Ctrl+V` 粘贴
5. `Ctrl+S` 保存
6. 油猴列表里出现「星脉自动充值」就成功了

> 注意：用粘贴方式装的这一份**不会自动更新**。以后出新版本要再粘一次。能用链接装上的，就不要用这个办法。

### 情况三：链接打不开

- 公司网络打不开 GitHub 时，安装链接会失败。让负责人把脚本内容发给你，按「情况二」第 3 步往后做
- 必须用 **Chrome 或 Edge**。360 浏览器、QQ 浏览器经常装不了油猴

### 装完怎么确认成功

打开京小洁「广告投放明细」页面，右下角出现蓝色 **工作台 5.9.3** 按钮，就说明装好了。

---

## 三、第一次使用：先做这两件事

### 1. 填店铺名称

工作台左侧点 **运行设置**。

在「店铺名称」右边点 **从页面抓取**。

**一定要用「从页面抓取」，不要手打。** 少一个字、多一个空格，脚本就找不到子账号，整天都不会充值。

抓取要求当前页面已经搜索出店铺。如果提示抓不到，先在京小洁页面点搜索，看到店铺那一行之后再抓。

### 2. 填钉钉机器人（可选）

同一页「钉钉机器人」里，把钉钉群自定义机器人的 Webhook 链接粘贴到 **机器人链接**。

- 机器人开了加签，就再填 **加签密钥**
- 机器人设了关键词，把 **关键词** 改成一致的（默认「自动充值」）
- 填完点 **测试钉钉通知**，群里收到消息就说明配好了

不需要通知的话，把 **开启钉钉通知** 取消勾选即可。

---

## 四、按顺序设置预算（左侧 ① 到 ⑤）

左侧导航是按顺序排的，从上往下一步一步做。**额度是从大到小一级一级往下分的**：店铺 → 时段 → 分组 → 单个账号。

### ① 店铺预算：先定今天全店能花多少

两种策略只能选一个：

- **智能店铺预算**：用近七天平均业绩和费比算出当天金额
- **固定店铺预算**：你自己手写当天预算金额

智能模式填这三个数：

| 填什么 | 说明 | 举例 |
|---|---|---|
| 近七天平均业绩 | 店铺近 7 天平均成交额 | 108800 |
| 推广+退货合计费比 % | 你允许的推广费加退货占比 | 18 |
| 店铺退货率 % | 店铺实际退货率 | 8 |

脚本算出：**当天预算 = 近七天平均业绩 ×（合计费比 − 退货率）**

上面这组数就是 108800 ×（18% − 8%）= **10880 元**。

固定模式只填「当天固定预算」，例如直接写 5200。

「店铺投产达标值」是允许超预算的门槛：店铺整体投产达到这个数时，允许突破当天预算继续充。不想让它超，就把这个值填得很高（比如 99）。

填完点 **保存预算**，再点右下角 **下一步 · 分时预算**。

### ② 分时预算：控制一天里的花钱节奏

页顶有一个 **开启分时预算** 开关。关掉后，全店只按第 1 步的当天店铺预算跑，不再被时段上限卡住。时段表会保留，下次打开继续用。

开启时，这里是**累计**上限，不是各段相加。默认四段：

| 时段 | 累计上限 | 含义（按当天预算 10880 元算） |
|---|---|---|
| 00:00-09:00 | 15% | 到 9 点前，累计最多花 1632 元 |
| 09:00-14:00 | 45% | 到 14 点前，累计最多花 4896 元 |
| 14:00-18:00 | 60% | 到 18 点前，累计最多花 6528 元 |
| 18:00-24:00 | 100% | 全天最多花 10880 元 |

时段和比例都能改，可以 **新增时段**、**删除**，改乱了点 **恢复默认**。

要求：时段首尾相接、比例逐段变大、最后一段到 24 点。不符合会给提示。

改完点 **保存分时规则**。

### ③ 子账号与分组：把账号拉下来分类

点 **从页面拉取子账号**。要求当前在京小洁投放明细页，并且店铺子账号已经展开；没展开就先点右上角 **立即执行全流程** 跑一轮，再来拉取。

拉下来后每行显示余额、今日花费、投产。之后每轮自动查询都会自动更新这些数字。

分组做法：

1. 下面「分组管理」里输入分组名，比如 `搜索类`，点 **新增分组**
2. 上面表格勾选要归到这一组的账号
3. 在 **移入分组…** 里选中该分组，点 **应用**

不分组也能用，那些账号会留在「未分组」，只受店铺预算和分时上限约束。

顺手可用的两个按钮：**为选中账号生成充值规则**（在第 ⑤ 步继续调）、**从名单移除**。

### ④ 分组与账号预算：给每组、每个账号分钱

这一页已改名为 **分组与账号预算**。分组按金额填写，**所有分组金额相加应对齐当天店铺预算**。

**分组预算**，三种选法：

- **填写金额**：直接写这一组今天能花多少，例如 2000
- **按店铺预算百分比**：填 30，就是当天预算的 30%（10880 × 30% = 3264 元）
- **不限制**：这一组不单独限额，只受店铺预算（以及开启时的分时上限）约束

分时开启时，分组当天预算还会按当前时段比例使用，和店铺分时一样。分时关闭后，分组按全天金额使用。

**单个账号预算**（下面那张表）：

- **当天上限**：这个账号今天最多充多少，留空＝不限
- **一次充值**、**投产高于**：留空＝跟随第 ⑤ 步的规则（账号规则 > 分组规则 > 全店规则）

改完点 **保存分组预算**。

各分组加起来对不齐店铺预算时，会给一条橙色提示，但**允许保存**。实际充值还是以上一级为硬上限，谁先触发谁先用。

### ⑤ 充值规则：什么时候充、充多少

规则分成三级，同一个号只走最细的一条：

1. **全店规则**：本店所有账号通用
2. **分组规则**：选一个账户组，这个组里的账号共用这条
3. **账号规则**：只对指定账号生效（精确 > 前缀 > 包含）

默认有一条「全店默认规则」：余额低于 50 且投产高于 2.2 就充 100 元。某个组或某个号需要不同门槛时，再单独加分组规则或账号规则。

常改的几项：

- **智能充值**：勾上后，余额低、投产够就自动补
- **一次充值**：命中后充多少钱
- **余额低于**：余额低于这个数才考虑充
- **投产高于**：投产高于这个数才充
- **定时充值**：勾上后，到你写的钟点再充，例如 `10:58=100`
- **分时充值**：勾上后，智能充值按时段用不同的金额和投产要求
- **冷却分钟**：同一个账号多久内不重复充

改完点 **保存规则**。

---

## 五、两条生效顺序（重要）

**店铺预算有两种，只能选一个：**

- **智能店铺预算**：当天预算 = 近七天平均业绩 ×（合计费比 − 退货率）
- **固定店铺预算**：你手写当天金额

**分时预算可以关掉。** 关掉后全店只按当天店铺预算跑，不再被时段上限卡住。

**能充多少钱，从大到小卡：**

店铺当天预算 →（若开启）当前分时上限 → 所在分组预算 → 账号当天上限

分组预算按金额填写，所有分组相加应对齐当天店铺预算。分时开启时，分组当天预算还会按当前时段比例使用。

**什么时候充、一次充多少，谁细谁说话：**

账号规则 > 分组规则 > 全店规则

同一个号只走最细的一条。

---

## 六、日常怎么用

1. 上班后打开京小洁「广告投放明细」页面，**这个页面要一直开着**，关掉脚本就停了
2. 打开工作台看 **总览**：当天预算、当前时段上限、已用、还能充多少、各分组进度
3. 想立刻跑一轮，点右上角 **立即执行全流程**
4. 脚本会自己打开京准通「分配金额」页去充值，**这个标签页尽量留在前台**，充完也别关。放在后台时浏览器会拖慢脚本，可能出现「到了充值页却不填金额」
5. 想知道有没有充、为什么没充，看 **提交记录**

正常情况下不用一直盯着，它每 10 分钟自己跑一轮。查询间隔可以在「运行设置」里改。

---

## 七、看懂「提交记录」

这一页分四块，从上到下：

**本轮未充值原因**：最近一轮扫描里，每个没充的账号是什么原因。下一轮会覆盖，只看当下。

**当天未充值原因统计**：今天各种原因各出现多少次，比如「余额未达门槛 86 次」。

**未充值原因变化**：某个账号原因变了才记一条，比如从「余额未达门槛」变成「超出分组额度」。原因没变只累加「持续轮数」。

**充值动作日志**：真实动作，已提交、失败、跨天重置。可以按「已充值 / 失败 / 系统」筛选。

常见原因怎么读：

| 看到这句 | 意思 | 要不要处理 |
|---|---|---|
| 余额 86.20 ≥ 50，未达到充值门槛 | 余额还够，不需要充 | 正常，不用管 |
| 投产 2.10 ≤ 2.5，投产不达标 | 投产太低，不给它加钱 | 正常，效果差就该少投 |
| 被第 2 级分时上限挡住 | 这个时段的额度用完了 | 想继续充就提高该时段比例 |
| 被第 3 级分组额度挡住 | 这一组的钱用完了 | 提高该组额度，或从别组挪 |
| 被第 4 级账号当天上限挡住 | 这个账号今天到顶了 | 需要的话调高它的当天上限 |
| 距上次充值 12 分钟，仍在 30 分钟冷却内 | 刚充过，防重复 | 正常 |
| 京准通分配页未找到同名账号 | 账号名对不上或页面没加载好 | 见第八节 |
| 跨天重置 | 到 0 点了，当天额度重新开始 | 正常 |

---

## 八、遇到问题怎么办

**安装时停在 Tampermonkey 的英文中转页，装不上**
- 见第二之二节「安装失败怎么办」

**右下角没有「工作台」按钮**
- 确认油猴扩展是开启状态
- Chrome 要打开「允许用户脚本 / 开发者模式」
- 刷新页面；确认当前是京小洁「广告投放明细」或京准通「分配金额」页

**提示「尚未填写店铺名称」**
- 到「运行设置」点 **从页面抓取**

**一个子账号都拉不到**
- 九成是店铺名称不对。清空后重新 **从页面抓取**，不要手打
- 也要确认页面已经搜索并展开了店铺的子账号

**整天都不充值**
- 先看「提交记录」里写的原因，按上一节的表对照
- 检查「运行设置」里 **暂停自动充值** 和 **模拟运行** 是不是勾上了
- 检查 ① 店铺预算是不是填了、是不是已经用完

**充值页一直不动**
- 京准通「分配金额」页要保持打开和登录状态
- 该页面被关掉后，脚本下一轮会重新打开

**命中了规则，到了京准通却不填金额**
- 先看「充值队列」里的 **充值页状态**，以及京准通页左上角蓝条，停在哪一句就对哪一句
- 把京准通这个标签页点到最前面，不要让它停在后台
- 充值时工作台会自动收起，避免自己的表格挡住查找；需要看设置再点右下角打开
- 同一个京准通只留一个「分配金额」标签页，多开了会互相抢
- 蓝条如果写「金额框在，但数字没写进去」或「没有找到金额输入框」，把那一页整屏截图发负责人

**想先试试不真充钱**
- 「运行设置」勾上 **模拟运行**，脚本只显示会充哪些账号，不真提交
- 结果在「充值队列」页的「模拟运行结果」里看

**想临时全停**
- 「运行设置」勾上 **暂停自动充值**

---

## 九、注意事项

- 设置保存在**你自己这台电脑的这个浏览器里**。换电脑、换浏览器要重新设置一次
- 同一个店铺不要在多台电脑同时开着自动充值，容易重复充
- 京小洁和京准通都要保持登录
- 脚本记录的是「已提交充值」，最终是否到账以京准通后台为准
- 每天 0 点后当天额度自动清零重新开始，「提交记录」里会有一条「跨天重置」
- 版本更新是自动的。当前版本看右下角按钮或工作台「版本中心」

---

## 十、新人最短上手路径

1. 装扩展 → 装脚本 → 删旧脚本
2. 打开京小洁投放明细页 → 点右下角 **工作台**
3. **运行设置** → 店铺名称点 **从页面抓取**
4. **① 店铺预算** → 选智能或固定其中一种 → **保存预算**
5. **② 分时预算** → 决定开或关；开启就用默认时段即可 → **保存分时规则**
6. **运行设置** → 勾 **模拟运行** → 右上角 **立即执行全流程** → 到「充值队列」看会充哪些
7. 觉得没问题 → 取消 **模拟运行** → 正式开始跑
8. 之后每天看 **总览** 和 **提交记录**

需要分组管理的再做 ③ 和 ④，不需要就跳过。
