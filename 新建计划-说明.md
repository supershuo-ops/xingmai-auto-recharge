# 星脉新建计划（京小洁批量建投放计划）

第二个油猴脚本，只负责在京小洁里批量新建投放计划。不充值，不去京准通转钱。

- 源码：`scripts/jxj-plan-builder/jxj-yanyuan-plan-builder.user.js`
- 脚本名：星脉新建计划
- 命名空间：`local.jxj.yanyuan.plan-builder`
- 当前版本：0.6.0（**默认演练模式，演练不会真的建计划**）
- 安装 / 自动更新地址：
  `https://raw.githubusercontent.com/supershuo-ops/xingmai-auto-recharge/main/jxj-yanyuan-plan-builder.user.js`

## 零、装一次就好，之后自动更新

私有仓的 raw 地址油猴拿不到（会 404），所以发布文件同步到公开仓
`supershuo-ops/xingmai-auto-recharge` —— 就是充值脚本用的那个，
两个脚本各占一个文件，互不覆盖，CI 的 token 也是同一个，不用另配。

| 文件 | 谁推的 |
|---|---|
| `jxj-yanyuan-auto-recharge.user.js` | 充值脚本，`sync-userscript.yml` |
| `jxj-yanyuan-plan-builder.user.js` | 本脚本，`plan-builder-ci.yml` |

**第一次安装**：在浏览器里打开上面那个地址，油猴会认出 `.user.js` 并弹出安装页，点「安装」。

**装过手动粘贴版本的，要先把那个删掉**，否则两份同名脚本会同时跑。
删掉不影响数据 —— 店铺名、任务表、日志存在油猴里，是按脚本名 + 命名空间存的，
只要这两样不变，重装、更新都不会丢。

**之后**：改动合进 `main`，CI 自动推到公开仓，油猴按自己的周期检查并更新。
想立刻更新就去油猴管理面板 →「已安装脚本」→ 右上角「检查更新」。

工作台左侧有「**更新日志**」页，每个版本改了什么都写在里面；有新版本时那一项会带个红色的「新」。

## 一、和充值脚本的关系

现有的「星脉自动充值」一行都没改。两个脚本的 `@name`、`@namespace`、存储键完全分开，
油猴当成两个脚本，设置各存各的。

**说好的用法是装在两个不同的浏览器里**：充值脚本常驻一个浏览器，
本脚本需要建计划时才开另一个。原因是两个脚本的工作页是同一个
（`jzt/adPlacementDetails`），装一起会互相干扰：

- 两边都会点「搜索」、扫表格、展开店铺行，而展开是个开关，被点两次就收回去了；
- 充值脚本把**任何含 `/jzt/` 的京小洁地址**都当成投放明细页
  （`detectPageMode()`，充值脚本第 7549 行附近），所以本脚本跳过去的快车页
  `/jzt/fastRide` 也会被它当成投放明细页去找「搜索」、扫表、滚页面，
  而本脚本那时正在填弹窗。

万一还是装到了一起，本脚本会在工作台首页红字提醒，并且自身留了几条防御：
面板一律用 div 布局不用 `table`/`tr`，面板里不出现文字正好是「搜索」「转入」的按钮，
找页面元素时把两边的面板都排除掉。

## 二、完整流程

先在工作台备好产品线、子账号和发布任务，再到执行页点开始。到了设定时间后，按子账号顺序逐个创建：

1. 在 `jzt/adPlacementDetails` 点「搜索」
   （左上店铺下拉和上面那排筛选**一律不碰**，保持当前状态）
2. 在「店铺名」列找到名字**完全一致**的店铺行，点行首箭头展开
3. 在展开的子账号里找到目标子账号（名字也要完全一致），点**子账号名称**
4. 页面跳到 `jzt/fastRide?shopName=…&accountName=…`
5. 切到「关键词」模式，点「新增计划」
6. 在弹窗里从上到下填：

   | 位置 | 动作 |
   |---|---|
   | 投放店铺 | 不动 |
   | 投放账户 | 下拉里选任务指定的子账号 |
   | 推广时间 | 不动 |
   | 计划名称 | 填发布任务里写的计划名称 |
   | 时段设置 | 不动（保持全天投放） |
   | 营销目标 / 营销场景 / 定向类型 | 都不动 |
   | 添加商品 | 粘 SKU → 手动添加 → 确定（**「全选」不用点**） |
   | 分配设置 | 选「默认」 |
   | 统一预算 | 按任务选「不限预算」或「自定义预算」+ 金额 |
   | 投放目标 | 选「成交」 |
   | 控制方式 | 改成「**投产比控制**」 |
   | 目标成交投产比 | 填任务里的投产比 |
   | 生效范围 / 地域定向 / 关键词设置 | 都不动 |

7. 点「确定」
8. 同一个子账号还有任务就接着建下一条；没有了就回投放明细页换下一个子账号

## 三、工作台怎么用

店铺名在「店铺设置」里填，页面右下角也会一直显示。**脚本只在京小洁页面开着时工作**，到了发布时间也必须保持投放明细页开着。

### 产品线

自定义产品线名称，每个产品线自己保存一份 SKU。支持：

- 直接在文本框里改
- 粘贴批量导入（一行一个，或逗号 / 空格分开）
- 从表格导入（先另存为 CSV / TSV / TXT，或把表格列粘过来；第一列是 SKU，表头是 sku / 商品 / 货号 时会自动跳过）

### 子账号信息

不要手填。打开投放明细页，点「从投放明细页拉取」，脚本会展开当前店铺并读出全部子账号及其它列，**不会点子账号**。拉回来之后可以分组；重新拉取会更新页面信息，原来的分组还在。

### 任务表（发布任务）

不再是「一行 = 一条计划」，而是先发一条任务：

1. 设置发布时间（留空 = 点开始后立刻跑）
2. 选择子账号组，或勾选指定子账号
3. 选择产品线（SKU 从产品线带过来）
4. 新增要创建的计划：投产比 + 计划名称

一条发布任务会按「子账号 × 计划」展开。同一个子账号的多条计划会连着建完，再换下一个子账号。

### 执行

到点后按顺序逐个创建。做完写「创建日志」，并按店铺设置发一条钉钉汇总。

## 四、演练模式

默认开着。演练模式下表单照样一步步填完，但最后点的是「**取消**」而不是「确定」，
不会真的建出计划。日志会写成「演练完成」，不要把它当成已经创建成功。
先用它跑一遍，确认每个字段都填对了，再到店铺设置里**关掉演练模式并保存**，再点「开始建计划」。

## 五、出错怎么办

某一步失败会先重试 2 次，还不行就把这条记成失败、跳过，接着建下一条，不会卡住整轮。
成功和失败都写进「创建日志」页（保留最近 500 条）。演练通过的记成「演练完成」，正式提交成功的才记成「已创建」。
全部跑完之后发一条钉钉汇总（机器人地址和加签密钥在工作台里填，不写进代码）。

## 六、上线前必须做的一件事

**页面元素的定位需要在真实页面上校准一遍。**

本脚本是照着截图写的，我没有真实环境的访问权限。京小洁的 class 名不稳定，
所以所有元素都是按「看得见的文字」找的（字段名、按钮文字、单选项文字），
这比写死 class 稳，但仍然有几处只能靠截图推断：

- 「自定义预算」的金额输入框长在哪 —— 截图里没展开过。
  现在的做法是记下点「自定义预算」之前有哪些输入框，点完找新冒出来的那个，
  再按几种布局兜底，应该能覆盖，但没在真页面上验证过。
- 子账号很多时要不要翻页 —— 现在只做了向下滚动查找。
- 提交之后的成功/失败提示是什么样 —— 现在是靠「弹窗关没关上」判断的。

建议第一次用的时候：**开着演练模式，任务表只放一行**，跑一遍看日志里每个字段是不是都
「已填」。有报错的把日志截图发来，我按真实页面调选择器。

## 七、还没定的

- 要不要和店铺预算联动。

## 八、开发

```bash
cd scripts/jxj-plan-builder
npm install
npm test        # 核心逻辑 + 投放明细页 + 快车页弹窗 + 发布任务工作流
npm run smoke
npm run build   # 生成 dist/ 混淆发布文件
PLAN_SCRIPT=dist/jxj-yanyuan-plan-builder.user.js npm run smoke
```

测试用的是一套会真实响应点击的假京小洁页面（`tests/fixtures.js`）：
展开店铺会真的出子账号，切「控制方式」会真的把「目标成交成本」换成「目标成交投产比」，
手动添加 SKU 会真的让「已选商品」计数变化。正式、演练、SKU 无效三条路径都覆盖到了。

发版规矩和充值脚本一样：**只提高 `@version` 和 `SCRIPT_VERSION`，并在 `VERSION_HISTORY` 里追加一条。**
测试会检查这两个版本号对不对得上，忘了加日志会直接失败。

**不要改 `@name` 和 `@namespace`**，改了油猴会当成新脚本：自动更新断掉，
本机的任务表和设置也全部读不到。
